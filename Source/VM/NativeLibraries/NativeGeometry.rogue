module RogueScript

$include NativeTriangle
$include NativeXY

class NativeGeometry [singleton]
  METHODS
    method register( vm:VM )
      NativeTriangle.register( vm )
      NativeXY.register( vm )
endClass

#local pet = Pet()
#println pet

class Dog : Pet
  METHODS
    #method speak
    #println "Woof!"
endClass

class Buffy : Dog
  METHODS
    method speak
      println "Rarararararar!"
endClass

local pet = Pet()
pet.speak
pet = Cat()
pet.speak
#pet = Buffy()
#pet.speak

#Beta().display

#{
class R2 : Random
  METHODS
    method next32->Int32
      #return prior.next32
      return 5

    method prior_next32->Int32
      return prior.next32

endClass

local r = R2()
println r.next32
println r.prior_next32
#local r0 = r : Random
#println r.next32

}#
